import { FormateHoraPipe } from './formate-hora.pipe';

describe('FormateHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
