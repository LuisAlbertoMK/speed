import { NombreSlicePipe } from './nombre-slice.pipe';

describe('NombreSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
