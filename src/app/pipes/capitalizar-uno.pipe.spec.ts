import { CapitalizarUnoPipe } from './capitalizar-uno.pipe';

describe('CapitalizarUnoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizarUnoPipe();
    expect(pipe).toBeTruthy();
  });
});
